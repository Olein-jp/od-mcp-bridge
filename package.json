{
  "name": "od-mcp-bridge",
  "version": "0.5.0",
  "private": true,
  "description": "Development tooling for the OD MCP Bridge WordPress plugin.",
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:status": "wp-env status",
    "env:logs": "wp-env logs",
    "env:cli": "wp-env run cli wp",
    "i18n:pot": "npm run env:cli -- i18n make-pot wp-content/plugins/od-mcp-bridge wp-content/plugins/od-mcp-bridge/languages/od-mcp-bridge.pot --domain=od-mcp-bridge --exclude=vendor,node_modules,tests,build",
    "package": "bash scripts/package.sh"
  },
  "devDependencies": {
    "@wordpress/env": "^10.36.0"
  }
}
