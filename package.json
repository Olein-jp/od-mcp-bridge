{
  "name": "od-mcp-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Development tooling for the OD MCP Bridge WordPress plugin.",
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:status": "wp-env status",
    "env:logs": "wp-env logs",
    "env:cli": "wp-env run cli wp",
    "package": "bash scripts/package.sh"
  },
  "devDependencies": {
    "@wordpress/env": "^10.36.0"
  }
}
